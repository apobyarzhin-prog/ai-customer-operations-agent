# Public URL of the deployed FastAPI service; do not put secrets in VITE_* values.
VITE_API_BASE_URL=https://api.example.com
# Enable only when the API allows credentialed CORS or is served same-origin.
VITE_COOKIE_AUTH=true
